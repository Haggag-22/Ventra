from gcp-* metadata _id, _index, _version | where gcp.audit.service_name=="login.googleapis.com" and (event.action in ("suspicious_login_less_secure_app", "suspicious_login", "suspicious_programmatic_login"))
