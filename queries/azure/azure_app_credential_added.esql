from * metadata _id, _index, _version | where azure.auditlogs.properties.activity_display_name in ("Update application – Certificates and secrets management", "Update Service principal/Update Application")
