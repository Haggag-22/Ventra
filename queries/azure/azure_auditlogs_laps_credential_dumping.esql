from * metadata _id, _index, _version | where azure.auditlogs.properties.category=="Device" and azure.auditlogs.properties.operation_type like "*Recover device local administrator password*" and azure.auditlogs.properties.additional_details like "*Successfully recovered local credential by device id*"
