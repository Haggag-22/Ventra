from * metadata _id, _index, _version | where azure.auditlogs.properties.activity_display_name=="Admin registered security info" and azure.auditlogs.properties.result=="Admin registered temporary access pass method for user"
