from * metadata _id, _index, _version | where aws.cloudtrail.event_source=="s3.amazonaws.com" and aws.cloudtrail.event_name=="PutBucketVersioning" and aws.cloudtrail.flattened.request_parameters like "*Suspended*"
