from * metadata _id, _index, _version | where aws.cloudtrail.event_source=="ds.amazonaws.com" and (aws.cloudtrail.event_name in ("CreateMicrosoftAD", "ConnectDirectory", "CreateTrust", "ShareDirectory", "AuthorizeApplication", "CreateConditionalForwarder"))
