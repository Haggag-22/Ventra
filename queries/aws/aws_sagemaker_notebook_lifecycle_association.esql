from * metadata _id, _index, _version | where aws.cloudtrail.event_source=="sagemaker.amazonaws.com" and (aws.cloudtrail.event_name in ("CreateNotebookInstance", "UpdateNotebookInstance")) and aws.cloudtrail.flattened.request_parameters like "*lifecycleConfigName*"
