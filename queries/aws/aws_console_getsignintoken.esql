from * metadata _id, _index, _version | where aws.cloudtrail.event_source=="signin.amazonaws.com" and aws.cloudtrail.event_name=="GetSigninToken" and not user_agent.original like "*Jersey/${project.version}*"
