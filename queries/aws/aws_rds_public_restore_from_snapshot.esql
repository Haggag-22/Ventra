from * metadata _id, _index, _version | where aws.cloudtrail.event_source=="rds.amazonaws.com" and aws.cloudtrail.event_name=="RestoreDBInstanceFromDBSnapshot" and aws.cloudtrail.flattened.response_elements like "*\"publiclyAccessible\":true*"
