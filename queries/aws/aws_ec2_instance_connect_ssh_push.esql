from * metadata _id, _index, _version | where aws.cloudtrail.event_source=="ec2-instance-connect.amazonaws.com" and aws.cloudtrail.event_name=="SendSSHPublicKey" or aws.cloudtrail.event_source=="ssm.amazonaws.com" and aws.cloudtrail.event_name=="StartSession"
