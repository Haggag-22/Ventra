from * metadata _id, _index, _version | where aws.cloudtrail.event_source=="signin.amazonaws.com" and aws.cloudtrail.event_name=="ConsoleLogin" and aws.cloudtrail.flattened.additional_eventdata like "*MFAUsed\":\"Yes*"
