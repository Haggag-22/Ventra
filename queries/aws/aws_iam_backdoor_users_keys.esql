from * metadata _id, _index, _version | where aws.cloudtrail.event_source=="iam.amazonaws.com" and aws.cloudtrail.event_name=="CreateAccessKey" and not aws.cloudtrail.user_identity.arn like "*responseElements.accessKey.userName*"
