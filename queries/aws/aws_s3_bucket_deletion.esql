from * metadata _id, _index, _version | where aws.cloudtrail.event_source=="s3.amazonaws.com" and aws.cloudtrail.event_name=="DeleteBucket" and (aws.cloudtrail.error_code=="Success" or aws.cloudtrail.error_code is null)
