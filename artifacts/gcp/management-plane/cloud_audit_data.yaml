name: GCP.ManagementPlane.CloudAuditData
aliases:
- cloud_audit_data
- gcp.cloud_audit_data
cloud: gcp
category: ManagementPlane
description: "Cloud Audit Logs \u2014 Data Access (read/write operations and policy\
  \ denials)."
version: 1.0.0
collector: cloud_audit_data
type: cloud_audit_data
required_actions:
- logging.logEntries.list
parameters:
  since:
    type: datetime
    required: false
  until:
    type: datetime
    required: false
sources:
- type: cloud_audit_data
  format: json
severity: critical
estimated_volume: medium
