name: GCP.DataStorage.SecretManager
aliases:
- secret_manager
- gcp.secret_manager
cloud: gcp
category: DataStorage
description: Secret Manager access audit logs from Cloud Logging Data Access.
version: 1.0.0
collector: secret_manager
type: secret_manager
required_actions:
- logging.logEntries.list
parameters:
  since:
    type: datetime
    required: false
  until:
    type: datetime
    required: false
sources:
- type: secret_manager
  format: json
severity: critical
estimated_volume: low
