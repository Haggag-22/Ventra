name: GCP.Network.ApiGateway
aliases:
- api_gateway
- gcp.api_gateway
cloud: gcp
category: Network
description: API Gateway request and gateway logs.
version: 1.0.0
collector: api_gateway
type: api_gateway
required_actions:
- logging.logEntries.list
parameters:
  since:
    type: datetime
    required: false
  until:
    type: datetime
    required: false
sources:
- type: api_gateway
  format: json
severity: extended
estimated_volume: medium
