name: GCP.Network.LoadBalancer
aliases:
- load_balancer
- gcp.load_balancer
cloud: gcp
category: Network
description: Load Balancer Access Logs for HTTP(S) and TCP/UDP proxy load balancers.
version: 1.0.0
collector: load_balancer
type: load_balancer
required_actions:
- logging.logEntries.list
parameters:
  since:
    type: datetime
    required: false
  until:
    type: datetime
    required: false
sources:
- type: load_balancer
  format: json
severity: critical
estimated_volume: medium
